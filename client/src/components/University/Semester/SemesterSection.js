import styled, { keyframes } from "styled-components"
import { Row } from "react-bootstrap"

export const SemesterContainer = styled(Row)`
    border: 2px solid #fff;
    text-align: center;
    color: #fff;
    padding: 5px;
`
